from turtle import *

def main():
    square(100)

def square(length):
    shape('turtle')
    forward(length)
    left(90)
    forward(length)
    left(90)
    forward(length)
    left(90)
    forward(length)

main()
